from fastapi import APIRouter

api_router = APIRouter()

# Exemplo de como incluir na rota abaixo
# api_router.include_router(atleta, prefix='/atletas', tags=['atletas']) | Precisa de um controller pra isso
